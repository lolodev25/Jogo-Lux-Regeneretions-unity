using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Events;



[RequireComponent(typeof(Collider2D))]
public class interacaopuzzle : MonoBehaviour
{
    /*
    [SerializeField]
    private anjoPuzzelControl joginterage;


    private UnityEvent botaoapert;

    private bool podeExecutar;

    void Update()
    {
        if (podeExecutar)
        {
            if (joginterage.Estainterag == true)
            {
                botaoapert.Invoke();

            }
        }
    }

    private void OnTriggerEnter2D(Collider2D collision)//dentro do colisor
    {
        podeExecutar = true;
    }

    private void OnTriggerExit2D(Collider2D collision)
    {
        podeExecutar = false;
    }
    */

}
